 const element = document.getElementById("downId");
        if (element.className == "right fas fa-angle-right") {
            element.className = "right fas fa-angle-down";
          } else {
            element.className = "right fas fa-angle-right";
          }
